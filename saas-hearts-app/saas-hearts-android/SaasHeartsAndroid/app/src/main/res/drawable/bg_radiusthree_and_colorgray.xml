<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--    solid : 背景填充-->
    <!--    corners ：圆角 (可单独设置圆角）-->
    <!--    stroke ：边框（可设置边框宽度和颜色）-->
    <!--    padding ：内边距-->
    <!--    gradient ：渐变色（背景）-->
    <!--    size ：大小-->
    <solid android:color="@color/ef"/>
    <corners android:radius="3dip"/>
    <stroke android:width="0dp" android:color="@color/ef"/>
    <padding
        android:left="0dp"
        android:top="10dp"
        android:right="0dp"
        android:bottom="10dp" />
</shape>
